# Settings to manage and configure a Firebase App Hosting backend.
# https://firebase.google.com/docs/app-hosting/configure

runConfig:
  # Increase this value if you'd like to automatically spin up
  # more instances in response to increased traffic.
  maxInstances: 1
  # The minimum number of instances to keep running.
  # minInstances: 0

# Defines a schedule for a job to run.
# schedule:
#   # The cron schedule for the job.
#   # https://cloud.google.com/scheduler/docs/configuring/cron-job-schedules
#   schedule: 'every 5 minutes'
#   # The number of seconds to wait for an HTTP request to complete.
#   timeoutSeconds: 300
#   # The number of times to retry a failed job.
#   retryCount: 3

# Defines environment variables that will be available to your App Hosting
# backend. In general, you should store sensitive data (such as API keys)
# in Secret Manager.
# https://firebase.google.com/docs/app-hosting/configure-backend#secret-manager
# env:
#   - variable: API_KEY
#     secret: SECRET_API_KEY # The secret in Secret Manager
#   - variable: ENVIRONMENT
#     value: production
